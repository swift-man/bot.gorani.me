# AGENTS.md

## Markdown Style Guide

- 새로운 `.md` 파일을 생성할 때는 이 저장소에 이미 존재하는 `.md` 파일의 스타일을 먼저 참고한다.
- 우선 참고 대상은 `README.md`, `src/data/post/*.md`, `src/pages/*.md`이다.
- 블로그 포스트 성격의 문서는 기존 포스트처럼 frontmatter를 먼저 작성하고, 이후 본문은 제목, 이미지, 구분선, 설명 순서를 최대한 동일한 톤으로 맞춘다.
- 문체는 기존 문서처럼 간결하고 직접적으로 유지하며, 불필요하게 다른 서식이나 과한 장식을 추가하지 않는다.
- 제목 깊이, 리스트 형식, 이미지 표기 방식, 구분선 사용 여부도 기존 문서와 일관되게 맞춘다.
- 새로운 Markdown 문서를 작성할 때 별도 지시가 없으면, 저장소 내 기존 `.md` 파일의 구조와 표현 방식을 기본값으로 사용한다.
